import * as breeze from 'breeze-client';
export declare class UriBuilderJsonAdapter implements breeze.UriBuilderAdapter {
    name: string;
    constructor();
    static register(config?: breeze.BreezeConfig): UriBuilderJsonAdapter;
    initialize(): void;
    buildUri(entityQuery: breeze.EntityQuery, metadataStore: breeze.MetadataStore): string;
}
